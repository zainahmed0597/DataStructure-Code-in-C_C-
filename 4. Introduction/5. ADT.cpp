#include <iostream>
using namespace std;

/*  1:Abstract DataType  called ADT
    ->Representation of Data 
    ->Operation on Data
    .add(element)/append(element)
    .add(index,element)/Insert(index,element)
    .remove(index)
    .set(index,element)/Replace(index,element)
    .get(index)
    .search(key)/Contains(key)
    .sort()
    */
    
int main(){
}
